# Care Quest リスク登録簿

最終更新: 2026-07-10 JST

PMP リスクマネジメントを1人+AI体制向けに軽量化した形式で運用します。
**見直しルール**: プランナー(Fable)はバッチ立案時に本登録簿を確認し、状態の更新・新リスクの追記を行う(`docs/project-management.md` バッチ立案チェックリスト 2 と整合)。

---

## リスク一覧

| ID | リスク | 影響度 | 発生確率 | 対応戦略 | 具体的対応 | オーナー | 状態 |
|---|---|---|---|---|---|---|---|
| R-01 | 個人情報(介護記録)の漏えい | 高 | 低 | 軽減 | Cognito 認証 + CORS を本番オリジン限定に設定。DynamoDB に個人識別情報は最小限のみ保存。プライバシーポリシーで収集範囲を明示。セキュリティ変更前に `/security-review` を実施。漏洩の疑いは `docs/runbook.md` 4 節のエスカレーション手順に従い即時人間へ報告 | 人間 | 対応中(Phase 0 ハードニング) |
| R-02 | localStorage 消失によるユーザーデータ喪失(端末変更・ブラウザデータ削除) | 高 | 中 | 軽減 | MVP は localStorage+サインインなし運用が前提のため「消える可能性あり」をオンボーディングで明示。Phase 1 で AWS 同期(US-103)を実装してサインイン時のバックアップを提供。将来は JSON エクスポート(US-503)も追加 | プランナー | 承認済み・軽減策をロードマップ化 |
| R-03 | 相談窓口情報(電話番号)の陳腐化 — 誤った番号の掲載は実害になる | 高 | 中 | 軽減 | リリースチェックリスト 2-A に「公式サイトで要確認」として明記済み。リリースのたびに人間オーナーが確認。About 画面・コードにも URL 出典コメントを残し変更検知を容易にする。誤番号を発見した場合は緊急変更として即日修正 | 人間 | 対応済み(チェックリストに組込) |
| R-04 | 単独運営のバス係数(オーナー不在時に運用が止まる) | 高 | 中 | 軽減 | `docs/` と `.claude/skills/` を SSOT として整備し、知識を属人化しない。`docs/runbook.md` に初動手順を文書化。緊急変更は人間オーナーが事後承認でも対応できるよう手順を明文化。GitHub Actions による自動デプロイで人間の手動操作依存を最小化 | 人間 | 対応中(ドキュメント整備継続) |
| R-05 | AWS コストの逸脱(特に将来の Bedrock 導入時) | 中 | 中 | 軽減 | AWS Budgets で月 10 USD アラーム設定済み(`CareQuestMonthlyBudget`)。Bedrock 導入(US-203)前に 1ユーザーあたりの生成回数上限をコードで強制し、追加予算アラームを設定。CloudWatch の Lambda/API Gateway メトリクスで異常トラフィックを早期検知。スパイク検知時は `docs/runbook.md` 1-C を参照 | プランナー | 対応中(Budgets 設定済み・Bedrock 導入は Phase 2) |
| R-06 | 励まし文言が特定の心理状態のユーザーに有害に働くケース(危機時に励ましだけで終わる) | 高 | 低 | 軽減 | エネルギーレベル「low」が連続した場合に相談窓口(介護者の会・自治体窓口)をそっと表示する仕様を US-502 で定義。Bedrock 連携(US-203)では医療的アドバイスを含まないようプロンプトで制約し、実装前に人間オーナーがレビュー。トーンガイド(`design-principles.md`)で「責めない・比較しない・励ますだけで終わらない」を原則化 | プランナー | 承認済み・US-502 で軽減策を実装予定 |
| R-07 | PWA Service Worker のキャッシュ不整合(古いコードが配信され続ける) | 中 | 中 | 軽減 | リリースチェックリスト 2-A に `public/sw.js` の VERSION インクリメントを必須項目として明記。ロールバック時も VERSION をさらに上げる手順を `release-checklist.md` 3節に定義。2-C でデプロイ後に `DevTools → Application → Service Workers` で activated を目視確認 | プランナー | 対応済み(チェックリストに組込) |
| R-08 | Bedrock 生成メッセージが不適切な内容を返す(ハルシネーション・不快表現) | 高 | 低 | 軽減 | US-203 受け入れ条件にプロンプト制約・ガードレール検証・テンプレートフォールバックを明記。Phase 2 導入時に `/security-review` と人間レビューを実施。初期はサンプリング確認期間を設け、問題発生時はフォールバックに即切り替え。導入前に医療アドバイス除外のシステムプロンプトをオーナーが承認 | 人間 | 未対応(Phase 2 で対処) |
| R-09 | 家族共有機能(US-402)の認可設計の不備による他家族データの参照 | 高 | 低 | 回避 | Phase 2 で実装するまでは機能を提供しない(回避)。実装時は Cognito グループ + DynamoDB の認可を `/security-review` と人間承認を必須とした通常変更として扱う。ふりかえりメモ(内心)は共有しないことを設計段階で確定し、US-402 受け入れ条件に明記済み | 人間 | 未対応(Phase 2 で対処) |
| R-10 | 自動テスト不在による回帰(ワーカー並列開発でのデグレ) | 中 | 高 | 軽減 | 第4バッチでユニットテスト(T18)と E2E スモーク(T21)を導入し CI で常時実行。DoD の lint/build に test を追加。実例: UTC 日付バグ(T17)はテストがあれば設計時に検知できた | プランナー | 対応中(第4バッチで対処) |

---

## 状態の定義

| 状態 | 意味 |
|---|---|
| 未対応 | 対応策未実施 |
| 対応中 | 対応策が進行中または一部実施済み |
| 承認済み・〜 | リスクを受容または対応策をロードマップ化した |
| 対応済み | 対応策が完了し、再発防止の仕組みが整っている |
| 監視中 | 対応済みだが定期的な確認が必要 |

---

## 見直し履歴

| 日付 | 更新者 | 内容 |
|---|---|---|
| 2026-07-10 | プランナー(T16) | 初版作成。指定6件 + 追加3件(R-07〜R-09)を登録 |
| 2026-07-10 | プランナー(第4バッチ立案) | R-10(回帰リスク)追加。コード点検で UTC 日付バグを発見し T17 として起票(欠陥のため登録簿ではなくタスクで管理) |
